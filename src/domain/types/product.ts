export interface ProductProps {
    productId?: string,
    name?: string,
    guidePrice?: number,
    costPrice?: number,
    productCategoryId?: string,
    productCategoryPath?: string,
    defaultImgUrl?: string,
    version?: number,
}

