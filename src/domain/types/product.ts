export interface ProductProps {
    productId?: string,
    name?: string,
    guidePrice?: number,
    costPrice?: number,
    productCategoryId?: string,
    defaultImgUrl?: string,
    version?: number,
}

